# Tello Web Interface

Utilizing flask and Tello (designed by CODE4FUN) I designed a (scuffed) interface so that a user can design a "flight plan", for the drone saved to a local sqlite3 database. 

> ```bash
> pip install tello
> pip install flask
> pip install sqlite3
> pip install opencv-python
> pip install pillow
> pip install pygame
>```